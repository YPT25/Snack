using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Mirror;

public class Debuff_Player : MPlayerBase
{
    [Header("ÉfÉoÉtê›íË")]
    [SerializeField] private float debuffDuration = 3.0f;
    [SerializeField] private float damegeInterval = 1.0f;
    [SerializeField] private float damageAmount = 5.0f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
